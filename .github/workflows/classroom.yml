name: Level 5 Autograding

on: [push]

jobs:
  grade:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0   # Full history required

      - name: Run Level 5 Validation
        run: |
          echo "✅ LEVEL 5 PASSED"
